# Sienna dashboard partition table — "Huge APP (3MB No OTA/1MB SPIFFS)".
#
# Why: the firmware links NimBLE + Wi-Fi + WebSocket + OV2640 camera +
# dual-I2S audio. That binary (~1.41 MB) overflows the stock 1.25 MB app0
# slot of the default dual-OTA scheme ("text section exceeds available
# space in board"). This single-app, no-OTA layout gives the app 3 MB.
#
# arduino-esp32 auto-detects a sketch-local partitions.csv and uses it over
# the IDE menu. If a build still reports the 1.25 MB ceiling, set
# Tools -> Partition Scheme -> "Huge APP (3MB No OTA/1MB SPIFFS)" (or
# "Custom") to force it. Sized for 4 MB flash; valid on 4 MB or 8 MB.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x300000,
spiffs,     data, spiffs,   0x310000, 0xE0000,
coredump,   data, coredump, 0x3F0000, 0x10000,
